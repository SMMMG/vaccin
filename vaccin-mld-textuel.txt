specialite = (code_specialite INT, description VARCHAR(50));
ecole = (immatricule_ecole INT, nom VARCHAR(50), adresse VARCHAR(100), cp VARCHAR(8), ville VARCHAR(50), nombre_eleves INT);
enfant = (nom VARCHAR(50), prenom VARCHAR(50), dateNaissance DATE, sex VARCHAR(1));
departement = (code_dpt VARCHAR(50), nom VARCHAR(50));
taille = (id_taille INT, taille INT, date_taille DATE);
poids = (id_poids INT, date_poids VARCHAR(50), poids DOUBLE);
categorie_vaccin = (nom VARCHAR(50), description VARCHAR(50));
centre_medical = (immatricule_centre INT, nom VARCHAR(50), adresse VARCHAR(100), cp VARCHAR(8), ville VARCHAR(50), #code_dpt);
vaccin = (code_vaccin INT, decsription VARCHAR(50), #nom);
medecin = (code_complementaire_medecin CHAR(5), nom_medecin VARCHAR(50), prenom VARCHAR(50), #immatricule_centre*);
vacination = (#(code_complementaire_medecin, nom_medecin), #code_vaccin, #(nom, prenom), date_vaccination DATE);
tab_specialite_medecin = (#(code_complementaire_medecin, nom_medecin), #code_specialite);
tab_ecole_medecin = (#(code_complementaire_medecin, nom_medecin), #immatricule_ecole);
tab_ecole_enfant = (#immatricule_ecole, #(nom, prenom), annee INT);
tab_enfant_taille = (#(nom, prenom), #id_taille);
tabe_enfant_poids = (#(nom, prenom), #id_poids);
